# Experimental private plugin runtime

Vendored from the Repo Apps framework for Quick Log's private Todo integration. It validates fixed immutable plugin descriptors, builds project-base virtual URLs, maps safe artifact requests, names SHA-scoped caches, and coordinates Service Worker control/messages.

It does not expose credentials to remote modules or turn same-origin plugins into a sandbox. Keep this copy synchronized with `repo-apps/packages/plugin-runtime`.
